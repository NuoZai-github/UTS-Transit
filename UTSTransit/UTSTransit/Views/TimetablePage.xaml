<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:UTSTransit.ViewModels"
             xmlns:models="clr-namespace:UTSTransit.Models"
             x:Class="UTSTransit.Views.TimetablePage"
             x:DataType="vm:TimetableViewModel"
             Title="Schedule">

    <Grid RowDefinitions="Auto, *">
        <Label Text="Departures"
               Style="{StaticResource HeaderLabel}"
               HorizontalOptions="Start"
               Padding="24,24,24,12" />

        <CollectionView Grid.Row="1" ItemsSource="{Binding Schedule}" Margin="16,0">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:TimetableItem">
                    <Frame Margin="0,6" Padding="16">
                        <Grid ColumnDefinitions="*, Auto">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding RouteName}" FontAttributes="Bold" FontSize="16" TextColor="{StaticResource TextPrimary}"/>
                                <Label Text="{Binding DepartureTime}" TextColor="{StaticResource TextSecondary}" FontSize="14"/>
                            </VerticalStackLayout>
                            
                            <Label Grid.Column="1" 
                                   Text="{Binding Status}" 
                                   TextColor="{Binding StatusColor}" 
                                   FontAttributes="Bold"
                                   VerticalOptions="Center" 
                                   FontSize="14"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
