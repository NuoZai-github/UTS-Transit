<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:UTSTransit.ViewModels"
             xmlns:models="clr-namespace:UTSTransit.Models"
             x:Class="UTSTransit.Views.TimetablePage"
             x:DataType="vm:TimetableViewModel"
             Title="Schedule">

    <Grid RowDefinitions="Auto, *">
        <!-- Header with Date Selector (Mockup) -->
        <VerticalStackLayout Padding="24,24,24,12" Spacing="16" BackgroundColor="{StaticResource Surface}">
            <Label Text="Bus Schedule" Style="{StaticResource HeaderLabel}"/>
            
            <!-- Date Chips (Horizontal Scroll) -->
            <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                <HorizontalStackLayout Spacing="12">
                    <Border StrokeShape="RoundRectangle 20" BackgroundColor="{StaticResource Primary}" Padding="16,8" StrokeThickness="0">
                        <Label Text="Today" TextColor="White" FontAttributes="Bold"/>
                    </Border>
                    <Border StrokeShape="RoundRectangle 20" BackgroundColor="{StaticResource PageBackground}" Padding="16,8" StrokeThickness="0">
                        <Label Text="Tomorrow" TextColor="{StaticResource TextSecondary}"/>
                    </Border>
                    <Border StrokeShape="RoundRectangle 20" BackgroundColor="{StaticResource PageBackground}" Padding="16,8" StrokeThickness="0">
                        <Label Text="Weekend" TextColor="{StaticResource TextSecondary}"/>
                    </Border>
                </HorizontalStackLayout>
            </ScrollView>
        </VerticalStackLayout>

        <!-- Schedule List -->
        <CollectionView Grid.Row="1" ItemsSource="{Binding Schedule}" Margin="16">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:TimetableItem">
                    <Frame Margin="0,6" Padding="0" CornerRadius="16">
                        <Grid ColumnDefinitions="80, *, Auto" Padding="16">
                            <!-- Time Column -->
                            <VerticalStackLayout VerticalOptions="Center">
                                <Label Text="{Binding DepartureTime}" 
                                       FontSize="18" 
                                       FontAttributes="Bold" 
                                       TextColor="{StaticResource Primary}"/>
                                <Label Text="Departs" 
                                       FontSize="12" 
                                       TextColor="{StaticResource TextSecondary}"/>
                            </VerticalStackLayout>

                            <!-- Route Info -->
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Padding="16,0">
                                <Label Text="{Binding RouteName}" 
                                       FontAttributes="Bold" 
                                       FontSize="16" 
                                       TextColor="{StaticResource TextPrimary}"
                                       LineBreakMode="TailTruncation"/>
                                <Label Text="To Campus" 
                                       FontSize="13" 
                                       TextColor="{StaticResource TextSecondary}"/>
                            </VerticalStackLayout>
                            
                            <!-- Status Badge -->
                            <Border Grid.Column="2" 
                                    StrokeShape="RoundRectangle 8" 
                                    BackgroundColor="{StaticResource PageBackground}" 
                                    StrokeThickness="0"
                                    Padding="8,4"
                                    VerticalOptions="Center">
                                <Label Text="{Binding Status}" 
                                       TextColor="{Binding StatusColor}" 
                                       FontAttributes="Bold"
                                       FontSize="12"/>
                            </Border>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
