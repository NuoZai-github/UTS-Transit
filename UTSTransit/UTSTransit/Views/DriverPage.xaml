<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:UTSTransit.ViewModels"
             x:Class="UTSTransit.Views.DriverPage"
             x:DataType="vm:DriverViewModel"
             Title="我是司机">

    <ContentPage.Resources>
        <ResourceDictionary>
        </ResourceDictionary>
    </ContentPage.Resources>

    <VerticalStackLayout Spacing="20" Padding="30" VerticalOptions="Center">

        <Label Text="🚌 巴士追踪 - 司机端" 
               FontSize="24" 
               HorizontalOptions="Center" 
               FontAttributes="Bold"/>

        <Label Text="选择当前路线:" />

        <Picker SelectedItem="{Binding SelectedRoute}">
            <Picker.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>Route A (Dorm -> Campus)</x:String>
                    <x:String>Route B (Campus -> Dorm)</x:String>
                    <x:String>周末专线</x:String>
                </x:Array>
            </Picker.ItemsSource>
        </Picker>

        <Label Text="{Binding StatusMessage}" 
               TextColor="Blue"
               HorizontalOptions="Center" 
               HorizontalTextAlignment="Center"/>

        <ActivityIndicator IsRunning="{Binding IsBusy}" 
                           Color="Orange" />

        <Button Text="开始/停止 行程"
                Command="{Binding ToggleSharingCommand}"
                BackgroundColor="#512BD4"
                TextColor="White"
                HeightRequest="60"
                FontSize="18"/>

    </VerticalStackLayout>
</ContentPage>